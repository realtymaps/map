@import 'basic-letter-values'

$template-stage-margin = 1in

#mail-wizard
  #edit-template
    .template-stage-container
      position relative
      overflow scroll
      left (-($template-stage-margin / 2) + 0.33in)
      // height ($page-height / $wysiwyg-scale-factor)

    // .clear-template
    //   all initial
    //   * 
    //     all initial

    .template-stage
      // all initial
      // position absolute
      left 0
      top 0
      right 0
      bottom 0
      padding ($template-stage-margin / 2)
      background-color $gray_ligther
      // border-right 1px solid $gray
      width (($page-width * $wysiwyg-scale-factor) + $template-stage-margin)
      height (($page-height * $wysiwyg-scale-factor) + $template-stage-margin)
      //line-height initial

      .letter-editor
        box-shadow 4px 4px 20px #888888
        
      .macro-display
        background-color #46b8f1
        border-radius 3px
        margin-left 2px
        margin-right 2px

      // .form-control
      //   line-height inherit

      .ta-scroll-window > .ta-bind
        padding: 0
      
      .ta-root 
        height ($page-height / $wysiwyg-scale-factor)
        
      #recipient-address-window, #return-address-window
        overflow hidden
        
      // p
      //   margin 0
      //   padding 0
      //   font-size inherit
      //   line-height inherit
