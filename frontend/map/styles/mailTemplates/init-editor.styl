@import 'basic-letter-values'


#mail-wizard
  #edit-template
    .template-stage-container
      position relative
      overflow scroll
      // height ($page-height / $wysiwyg-scale-factor)

    // .clear-template
    //   all initial
    //   * 
    //     all initial

    .template-stage
      // all initial
      // position absolute
      left 0
      top 0
      right 0
      bottom 0
      padding 0.25in
      background-color $gray_ligther
      border-right 1px solid $gray
      width (($page-width / $wysiwyg-scale-factor) + 0.5in)
      height (($page-height / $wysiwyg-scale-factor) + 0.5in)
      //line-height initial

      .form-control
        line-height initial

      .ta-scroll-window > .ta-bind
        padding: 0
      
      .ta-root 
        height ($page-height / $wysiwyg-scale-factor)
        
      // p
      //   margin 0
      //   padding 0
      //   font-size inherit
      //   line-height inherit
