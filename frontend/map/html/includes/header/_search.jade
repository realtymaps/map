div.search-field
  form(ng-cloak ng-controller="rmapsSearchCtrl")
    button.btn.btn-icon.search-btn(ng-click="openSearchTools = !openSearchTools" )
      span.icon.icon-magnify
    div.search(ng-controller="DropdownCtrl" ng-class="openSearchTools ? 'opened' : ''")
      div.search-content
        div.search-type
          span.dropdown(dropdown on-toggle="toggled(open)")
            button.dropdown-toggle(href='#search-type' dropdown-toggle)
              span
                span.type
                  | {{searchScope}}
              span.caret
            ul.dropdown-menu
              li(ng-click="searchScope = 'Properties'")
                span.icon.icon-property
                | Properties
              li(ng-click="searchScope = 'Owners'")
                span.icon.icon-project
                | Owners

        input.search-input.form-control(
          ng-show="searchScope == 'Owners'"
          tooltip="{{searchTooltip}}"
          tooltip-placement="bottom"
          placeholder="Enter a name"
          ng-model="selectedFilters.ownerName"
          ng-change="searchChanged()")

        input.search-input.form-control(
          ng-show="searchScope == 'Properties'"
          disabled
          tooltip="{{searchTooltip}}"
          placeholder="Enter an adddress"
          ng-model="selectedFilters.address"
          ng-change="searchChanged()")
