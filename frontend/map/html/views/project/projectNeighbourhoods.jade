#project-neighborhood-container
  div(ng-repeat="neighbourhood in project.neighbourhoods")
    a.map
      span View Neighbourhood
      img(src="/assets/test_map.jpg")
    .data
      h3 {{neighbourhood.name}}
    button.btn.btn-link
      i.fa.fa-trash-o
      span Delete
