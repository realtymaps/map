section.white-page.onboarding.onboarding-plan(ng-cloak)
  .container
    div(ng-if="view.showSteps")
      a(ng-if="view.hasPrevStep" ng-click="view.goToPrevStep()")
        .pull-left.fa.fa-chevron-left.fa-3x()
      h3 Step {{view.currentStepId}} of {{view.steps.length}}
    
    div(ng-repeat="state in $state.get() | filter:{parent: 'onboarding'}"
      ui-view="{{state.name}}"
      ng-show="$state.includes(state.name)", ng-cloak
    )
