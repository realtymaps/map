include ../includes/navbar.jade

div(roll="main").adminWorkspace
  include ../views/home.jade
  div(ng-repeat="state in $state.get() | filter:{parent: 'main'}", ui-view="{{state.name}}", ng-show="$state.includes(state.name)", ng-cloak).adminPage

include ../../../common/html/includes/alerts.jade
