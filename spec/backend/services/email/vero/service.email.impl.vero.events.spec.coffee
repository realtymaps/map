require 'should'
basePath = require '../../../basePath'
subject = require "#{basePath}/services/email/vero/service.email.impl.vero.events"

describe "service.email.impl.vero.events", ->

  # it "makeVeroEvent", ->
  #   subject().makeVeroEvent("service.email.impl.vero.events")
  #   .should.be.equal "Service email impl vero events"
